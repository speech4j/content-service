# ===============================
# DATABASE
# ===============================
db:
  driver: org.postgresql.Driver
  host: localhost
  port: 5432
  name: content_db
  url: jdbc:postgresql://${db.host}:${db.port}/${db.name}
  username: postgres
  password: postgres